# Обязательное
Pillow>=10.0          # покадровая компоновка, субпиксельные наезды
playwright>=1.40      # рендер холста через Chromium

# Анализ материала
numpy>=1.26           # разбор звука, поиск пауз
opencv-python-headless>=4.9   # трекинг лица, авторамка

# Только на своей машине: в песочнице модели скачать неоткуда
faster-whisper>=1.0   # расшифровка речи, если нет готового SRT
